# Prometheus config for canister metrics
global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: "s7g-canister"
    static_configs:
      - targets: ["metrics-exporter:8000"]
    metrics_path: /metrics
